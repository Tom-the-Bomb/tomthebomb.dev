---
interface Props {
    title: string;
}

const { title } = Astro.props;
---

<div class="flex flex-col gap-3">
    <h2 class="w-fit border border-white px-2 text-lg font-bold">{title}</h2>
    <p>
        <slot />
    </p>
</div>
